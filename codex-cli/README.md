# Deprecated legacy CLI client

This package is retained only for repository history and is **not used in the supported codex-only architecture**.

Use the VS Code extension plus the mobile app or relay server instead.
